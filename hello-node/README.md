# hello-node

Dependencies:

* Node.js v6

Install the requirements:

```
npm install
```

Configure the application:

```
export HELLO_GO_ADDR = [ hello-go app address ]
```

Run the app localy:

```
node app.js
```
